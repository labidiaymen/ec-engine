// Direct file module
export const colors = {
    red: "#FF0000",
    green: "#00FF00",
    blue: "#0000FF"
};

export function getRandomColor() {
    // Simplified for ECEngine compatibility
    return "#FF0000"; // Always return red for now
}

export default colors;
