// Simple utility functions
export function greet(name) {
    return "Hello, " + name + "!";
}

export function capitalize(str) {
    // Simple capitalization for ECEngine compatibility
    return str; // TODO: Implement when string methods are available
}

export const version = "1.0.0";

export default {
    greet: greet,
    capitalize: capitalize,
    version: version
};
