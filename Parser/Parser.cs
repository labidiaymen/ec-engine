using ECEngine.AST;

namespace ECEngine.Parser;

// Stub parser for JavaScript code
public class Parser
{
    // Parse code and return AST (stub)
    public ASTNode Parse(string code)
    {
        // Stub: returns a NumberLiteral node for now
        return new NumberLiteral(3);
    }
}
