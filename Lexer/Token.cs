namespace ECEngine.Lexer;

// Enum for all token types
public enum Token
{
    Identifier,
    Number,
    Operator,
    LeftParen,
    RightParen,
    LeftBrace,
    RightBrace,
    Semicolon,
    EOF
}
